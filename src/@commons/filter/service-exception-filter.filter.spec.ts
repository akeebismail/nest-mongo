import { ServiceExceptionFilterFilter } from './service-exception-filter.filter';

describe('ServiceExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new ServiceExceptionFilterFilter()).toBeDefined();
  });
});
